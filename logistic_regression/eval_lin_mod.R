eval_lin_mod <- function(X, model) {
  # Computes a predicted value of the dependend variable given a particular linear model
  #
  # Args:
  #   X:      matrix of observations
  #   model:  vector of coefficients
  #   ASSUMES: ncol(X) equals length(model)
  #
  # Returns:
  #   vector of predicted values, one for each observation in X
  0
}